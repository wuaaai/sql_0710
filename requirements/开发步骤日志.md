# 统一意图识别模块 — 开发步骤日志

## 概述

基于 `统一意图识别模块设计.md`，在 AIAssistant 新框架中开发统一意图识别模块。

**原则**：严格按照设计文档逐节实现，日志与设计文档一一对应，每步确认后再继续。

---

## 步骤与设计文档对应关系

| 步骤 | 对应设计文档章节 | 内容 | 涉及文件 | 状态 |
|------|-----------------|------|----------|------|
| 1 | §3.2, §3.3 | 创建 `Text2SqlIntent` / `RagIntent` / `UnifiedIntentDict` 三个 dataclass | `src/domain/intent/__init__.py` `src/domain/intent/unified_intent.py` | 已完成 |
| 2 | §2, §7 | 创建 `UnifiedIntentExtractor` 类骨架（输入 `question/history_messages/region_code`，返回 `UnifiedIntentDict`） | `src/pages/request_lifecycle/unified_intent_extractor.py` | 已完成 |
| 3 | §5.1, §5.2, §6 第一阶段 | 实现 LLM 提取逻辑 — 复用 `intent.py:INTENT_PROMPT` + `build_query_plan()` 二阶段模式 | `unified_intent_extractor.py` | 已完成 |
| 4 | §5.1, §5.2, §6 第一阶段 | 实现规则兜底逻辑 — 迁移 `intent.py` 全部 `_extract_*()` 函数 | `unified_intent_extractor.py` | 已完成 |
| 5 | §5.3, §6 第二阶段 | 实现 `_build_rag_fields()` — 从 LLM 原始输出构造 RAG 子集（只解析 need_* + 透传原问题） | `unified_intent_extractor.py` | 已完成 |
| 6 | §3.1, §7 | 改造 `dispatch_execution.py` — 基于 `UnifiedIntentDict` 各链路自身字段推导调度策略 | `src/pages/request_lifecycle/dispatch_execution.py` | 已完成 |
| 7 | §4 | 联调测试 — 用 123 条 Excel 测试用例验证五种调度结果 | 测试脚本 + LlmGateway | 已完成 |

---

## 详细日志

### 2026-07-10

- 完成需求文档 `统一意图识别模块设计.md`
- 创建开发步骤日志（本文件）
- 完成步骤 1-7 的代码实现
- RagIntent 从 7 字段精简为 5 字段（删除 search_query / subject_keywords）
- RagIntent 构造不再依赖 Text2SqlIntent，两条链路彻底解耦
- 完成 123 条测试用例（去重后 81 条），100% 成功率

---
